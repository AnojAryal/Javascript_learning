let num  = 10;

let result

// if(num % 2 === 0)
//     result = "Even Number"

// else
//     result = "Odd Number"

//rather than doing this we can use ternary operators
// ?:

result = num % 2 === 0 ? "Even Number" : "Odd Number"

console.log(result);